<view class="sheet-mask {{show?'show':''}}" bindtap="onMaskTap">
  <view class="sheet {{show?'show':''}} {{night?'night':''}}" catchtap="noop">
    <view class="grip"></view>
    <slot></slot>
  </view>
</view>
